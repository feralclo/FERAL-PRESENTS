"use client";

import type { SectionProps } from "../types";

/**
 * Anchor the quest to an event so its share_url uses the event slug.
 * Phase 2.6 wires the event picker (next 3 upcoming + search) and the
 * closed-state header summary ("Event · Only Numbers London").
 */
export function EventSection({ state, onChange }: SectionProps) {
  void state;
  void onChange;
  return null;
}
