"use client";

import type { SectionProps } from "../types";

/**
 * The dry power-user controls in one place: max_completions per rep,
 * expires_at, auto_approve. Phase 2.8 fills this in with the closed-state
 * summary patterns (e.g. "Rules · 1 completion · expires Sat 5 May").
 */
export function RulesSection({ state, onChange }: SectionProps) {
  void state;
  void onChange;
  return null;
}
