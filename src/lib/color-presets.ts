/**
 * @deprecated — Use `@/lib/theme-vibes` instead.
 * This file re-exports for backwards compatibility.
 */
export {
  THEME_VIBES as COLOR_PRESETS,
  getVibeById as getPresetById,
  type ThemeVibe as ColorPreset,
} from "./theme-vibes";
